input LoginInput {
    email: String!
    password: String!
}
mutation Login($data: LoginInput!) {
    login(data: $data) {
        accessToken
        user {
            uuid
            email
            name
        }
    }
}