mutation Register($data: RegisterInput!) {
    register(data: $data) {
        user {
            uuid
            name
            email
        }
        emailConfirmation {
            uuid
            expiresAt
        }
    }
}
